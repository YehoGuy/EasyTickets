package com.example.easytickets.domain.model;

/**
 * Enumerates the three supported search entry points exposed on the home screen.
 */
public enum SearchMode {
    HOTEL,
    CITY,
    MY_LOCATION
}
